#include "../include/sample2.h"
int Sub_main1()
{
   char name[50];
   int marks, i=0, num=10;
   //printf("Enter number of students: ");
   //scanf("%d", &num);
   for(i = 0; i < num; ++i)
   {
      //printf("Hello world");
      //scanf("%s", name);
      //printf("Enter marks: ");
      //scanf("%d", &marks);
      //printf("\nName: %s \nMarks=%d \n", name, marks);
   }
   return 0;
}